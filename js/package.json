{
  "name": "wdf-server-js",
  "version": "1.0.0",
  "description": "Used to get the final HTML of a web page after JS execution",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kewin Dousse",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^0.13.0"
  }
}
